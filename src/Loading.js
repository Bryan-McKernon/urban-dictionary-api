import React from 'react';
import './Loading.css';

const Loading = (props) => {
    return (
        <div className="spinnerContainer">
            <div className="spinner"/>
        </div>
    )
}
export default Loading;